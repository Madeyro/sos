summary: Metadata used by tmt itself are valid
discover:
    how: shell
    tests:
      - name: /lint/tests
        require: tmt
        test: tmt tests lint
      - name: /lint/plans
        require: tmt
        test: tmt plans lint