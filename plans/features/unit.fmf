summary: Verify core functionality by running unit tests
description:
    Tun all unit tests
discover:
    how: shell
    tests:
      - name: /unit/tests
        test: PYTHONPATH=. avocado run tests/unittests/
        # test: PYTHONPATH=. avocado --help
prepare:
    how: shell
    script:
      - dnf -y remove sos
      - dnf -y install python3.7 python3-pip ethtool
      - pip3 install -r requirements.txt
      - pip3 install avocado-framework==94.0
