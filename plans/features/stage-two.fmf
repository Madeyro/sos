summary: Verify core functionality by running unit tests
description:
    Tun all unit tests
order: 60
discover:
    how: shell
    tests:
      - name: /stage/two
        test: PYTHONPATH=tests/ avocado run --test-runner=runner -t stagetwo tests/{cleaner,collect,report,vendor}_tests

prepare+:
    how: shell
    script:
      - dnf install -y python3-pexpect